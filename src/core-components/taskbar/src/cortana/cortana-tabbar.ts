import { h } from 'vue'

const componentName = 'f-taskbar-cortana-tabbar'

export default {
    name: componentName,
    setup() {
        
    },
    render() {
        return h("div", {
            id: componentName,
            
        })
    }
}