import { h } from 'vue'

const componentName = 'f-content-menu-divider'

export default {
    name: componentName,
    setup() {

    },
    render() {
        return h("div", {
            id: componentName,

        })
    }
}