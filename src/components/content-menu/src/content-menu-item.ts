import { h } from 'vue'

const componentName = 'f-content-menu-item'

export default {
    name: componentName,
    setup() {
        
    },
    render() {
        return h("div", {
            id: componentName,
            
        })
    }
}