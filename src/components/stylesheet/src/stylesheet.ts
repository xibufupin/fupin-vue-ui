import { h } from 'vue'

const componentName = 'f-stylesheet'

export default {
    name: componentName,
    setup() {
        
    },
    render() {
        return h("div", {
            id: componentName,
            
        })
    }
}