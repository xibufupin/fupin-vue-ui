import { h } from 'vue'

const componentName = 'f-taskbar-notify-item'

export default {
    name: componentName,
    setup() {
        
    },
    render() {
        return h("div", {
            id: componentName,
            
        })
    }
}